---
name: simplify
description: "Writes a code in a simple and understandable way without extra stuff like comments and docs."
disable-model-invocation: true
---

# Simplify

Instead of writing comments and documentations, simply make sure the code itself, the name of the functions and the parameters and arguments and variables are all written in a simple language that in it by itself explains what the code does.
If it needs comments or explanation the code itself is not clear enough.
